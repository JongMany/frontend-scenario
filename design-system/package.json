{
  "name": "design-system",
  "version": "0.0.0",
  "main": "index.js",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.6.0",
  "devDependencies": {
    "nx": "^20.4.3"
  }
}
